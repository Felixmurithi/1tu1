// import { useQuery } from "@tanstack/react-query";
// import { allDatesAction } from "@/app/_lib/action";

// // export default function useDates(lng, lat , userId) {
//   const { data: dates, isLoading: loading } = useQuery({
//     queryFn: allDatesAction(lng, lat, userId),
//     queryKey: ["dates"],
//   });

//   return { loading, dates };
// }
