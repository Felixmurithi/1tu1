## google oauth client

### Oauth consent screen

Initial set up is in set up mode

1. under API and services find Oauth conset screen
2. select external type
3. under ouath conenst screen- app name add support and developer email which are mandatory, can add logo or domain name
4. skips scopes
5. add test users whih will be able to use the aplication under text mode.

### credentials

1. Under api and services/ credentials add new Oauth client id
2. add domain and call back url without the root ("/")
3. copy the ID and secret into the env.local

Publish app after testing
